[build]
buildCommand = "pip install --no-cache-dir -r requirements.txt && python -m playwright install chromium --with-deps"

[deploy]
startCommand = "python main.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
